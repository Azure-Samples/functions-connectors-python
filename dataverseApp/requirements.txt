# Azure Functions dependencies
azure-functions>=2.2.0
# Managed-identity auth.
azure-identity
# Typed Dataverse (commondataservice) connector client used by the List rows action.
azure-connectors>=0.3.0b2
